# 案鉴视频工坊 · 架构文档 v2.0

## 核心理念

```
用户输入API Key（在本地存储，不发送到任何服务器）
         │
         ▼
    ┌────────────┐
    │  故事编辑   │  ← 手动写故事 / AI辅助生成
    └─────┬──────┘
          │ 分场景
          ▼
    ┌────────────┐
    │ TTS 语音生成 │  ← 支持：Web Speech(free) / 火山引擎 / OpenAI TTS / 百度AI
    └─────┬──────┘
          │ MP3/WAV
          ▼
    ┌────────────┐
    │ 画面生成    │  ← 支持：Canvas渲染(free) / DALL-E / 通义万相 API
    └─────┬──────┘
          │ PNG序列
          ▼
    ┌────────────┐
    │ FFmpeg合成  │  ← 浏览器端 FFmpeg.wasm
    └─────┬──────┘
          │ MP4
          ▼
      下载视频
```

## 前端页面

```
┌──────────────────────────────────────┐
│  ⚖️ 案鉴 · 判例故事视频工坊           │
│                                      │
│  ┌─────┐  ┌──────────┐  ┌────────┐  │
│  │ 登录  │  │ API密钥   │  │ 案例库  │  │
│  │ 注册  │  │ 管理面板   │  │ 编辑器  │  │
│  └─────┘  └──────────┘  └────────┘  │
│                                      │
│  ┌─────────────────────────────┐     │
│  │   视频制作流水线              │     │
│  │   Step 1: 选择/编辑故事       │     │
│  │   Step 2: 生成配音           │     │
│  │   Step 3: 生成画面           │     │
│  │   Step 4: 合成并下载         │     │
│  └─────────────────────────────┘     │
└──────────────────────────────────────┘
```

## 支持的API集成

### TTS 语音
| 服务 | API | 是否需要Key | 音质 |
|------|-----|------------|------|
| Web Speech API | 浏览器内置 | 免费不需要 | 一般（机器人感） |
| 火山引擎TTS | volc TTS API | 需要 | 好（自然） |
| OpenAI TTS | tts-1 / tts-1-hd | 需要OpenAI Key | 好 |
| 百度AI语音合成 | baidu TTS | 需要 | 好 |
| 阿里云语音合成 | aliyun TTS | 需要 | 好 |

### 画面生成
| 服务 | 方式 | 特点 |
|------|------|------|
| Canvas渲染（内置） | 纯前端 | 快速、免费、可控 |
| DALL-E 3 | OpenAI API | 高质量、有费用 |
| 通义万相 API | 阿里云 | 中文理解好 |
| 即梦AI API | 字节跳动 | 免费额度多 |

### 视频合成
| 方式 | 说明 |
|------|------|
| FFmpeg.wasm | 浏览器内纯前端合成，无需后端 |
| Canvas CaptureStream | 实时录制，质量有限 |

## 数据安全

- API Key 仅存储在浏览器 localStorage
- 所有 API 调用从前端直接发到对应服务商（不经过任何中间服务器）
- 用户注册信息也仅存在本地（离线可用）
